# Corne dongle — BLE central + USB host connection.
# Role, split, and peripheral count live in the shield's Kconfig.defconfig.

# Boost BLE TX power for solid range to both halves.
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_BT_CTLR_PHY_2M=n
